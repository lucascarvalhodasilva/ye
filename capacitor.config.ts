import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carvalhodasilva.fleetsteuer',
  appName: 'Fleet Steuer',
  webDir: 'out'
};

export default config;
